language: php




dist: bionic


php:
 - 7.3

 service:
 - mysql


 before_script:
  - cp .env.example .env
  - composer self-update
  - composer install --no-interaction --prefer-source
  - php artisan key:generate

script:
 - vendor/bin/phpunit

